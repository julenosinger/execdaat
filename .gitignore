# Dependencies
node_modules/

# Build outputs
dist/
dist-vercel/
.vercel/
.wrangler/

# IDE and Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Environment
.env
.dev.vars

# Treasury secrets — NEVER commit keys/keystores/mnemonics (operator-side only)
.treasury-secrets/
**/.treasury-secrets/
*.keystore.json

# Private keys and deployer credentials
.my-deployer.json
private-key.json
*.pem
*.key

# Deployment outputs (may contain addresses)
deployment.json

# Security scan artifacts
.security-scan-results.json

# Dependencies lockfiles (optional - uncomment if needed)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml
